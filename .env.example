# Copy to .env (not version-controlled) and fill with the real credentials
# of the RESTRICTED database user (read-only). Never commit the real .env.
GAUSSDB_HOST=
GAUSSDB_PORT=8000
GAUSSDB_USER=ai_agent_ro
GAUSSDB_PASSWORD=
GAUSSDB_DBNAME=
